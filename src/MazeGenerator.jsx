import { useEffect, useRef, useState } from "react";

class Maze {
    constructor(size, rows, columns) {
        this.side = size;
        this.cloumns = columns;
        this.rows = rows;
        this.grid = [];
        this.stack = [];
    }
}

class Cell {
    constructor(rownNum, colNum, parentGrid, parentSize) {

    }
}

export const MazeGenerator = () => {
    return (
        <>
        </>
    );
}